TODO
====

Game:

- Move car to the nearest track tile if stuck for too long
- Grandstands
- Global setting for shadow offsets
- Initial start menu
- Install target for CMake
- Full screen mode
- One big wall object
- Too easy to accidentally miss checkpoints
- Update the code to use VBO's.
- Collision filter for objects
  (e.g. trees doesn't need to check against other trees).

Content:

- More tracks
